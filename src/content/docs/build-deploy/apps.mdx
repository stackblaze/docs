---
title: Services
description: A service is a deploy target on the project canvas — Git, Docker, a template, a cron job, or a database.
---

import { Steps } from '@astrojs/starlight/components';

A **service** is a container StackBlaze builds and runs. Add one from the canvas with **+ Service**. Source is a Git repository, a Docker image, or a template. Databases are services on the same board.

Web, worker, and cron are **settings** on a service (public domain, no domain, or a schedule) — not separate products.

## Types of services

### Always-on

The default. The container stays running. Use this for HTTP APIs, frontends, queue consumers, and anything that must not exit.

- Give it a public domain for HTTP → [Public HTTP](/deploy/web-services/)
- Leave it private for workers → [Private services](/deploy/background-workers/)

### Scheduled

A [cron job](/deploy/cron-jobs/) runs until completion on a cron expression, then exits. Same build path as always-on services.

### Databases

From **+ Service → Databases**, provision Postgres, Redis/Valkey, MySQL, and other managed engines. They join the environment's [private network](/networking/private-networking/). Wire connection strings with [variables](/security/env-vars/) (`fromDatabase` in config as code).

## Create a service

<Steps>

1. Open a project. You are on the environment canvas (production unless you switched).

2. Click **+ Service** and pick a source:

   - **Git repository** — GitHub or a public git URL
   - **Docker images** — registry image
   - **Templates** — a pre-wired stack
   - **Databases** — managed engine
   - **Import** — Compose, `render.yaml`, or `fly.toml`

3. Review the [staged change](/deploy/staged-changes/) and **Deploy**.

</Steps>

## Service source

If a `Dockerfile` is in the repo, StackBlaze uses it. Otherwise Nixpacks detects the runtime from files such as `package.json` or `requirements.txt`. Override install, build, and start commands in the service sheet.

Change source later from the service **Settings** tab.

## Monorepos

Set a root directory so one repo can back several services. See [Monorepo](/cicd/monorepo/).

## Next steps

- [Variables](/security/env-vars/)
- [Staged changes](/deploy/staged-changes/)
- [Deploy from GitHub](/deploy/github/)
- [Config as Code](/blueprint/)
