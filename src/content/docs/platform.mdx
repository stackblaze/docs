---
title: Platform
description: What StackBlaze is, how a deploy works, and how to migrate an existing app.
---

import { LinkCard, CardGrid } from '@astrojs/starlight/components';

StackBlaze is an all-in-one deployment platform. You compose **services** and **databases** on a **project canvas**, deploy from Git or a Docker image, and get private networking, TLS, volumes, and observability without operating a cluster.

You work in the dashboard (or [MCP](/agents/) / the [API](/reference/api/)). You do not write Kubernetes manifests or run kubectl.

## How a deploy works

1. **Add a service** on the canvas from GitHub, a Docker image, a template, or an import.
2. **Stage and deploy.** The change sits on the canvas until you deploy.
3. **Build.** StackBlaze clones the commit (or pulls the image). A `Dockerfile` wins; otherwise Nixpacks detects the runtime.
4. **Rollout.** A new revision comes up behind a health check, then traffic moves over.
5. **URL.** Generate a `*.stackblaze.app` domain or attach a [custom domain](/networking/custom-domains/).

Every push to the connected branch can [auto-deploy](/cicd/auto-deploy/). Add `[skip ci]` to a commit message to skip.

## Mental model

| Piece | Role |
| --- | --- |
| Project | Canvas of services that share a private network per environment |
| Service | Git, image, cron, or database |
| Environment | Isolated copy of the canvas (production, custom, PR preview) |
| Variables | Config and secrets, including `fromDatabase` |
| Volume | Disk attached to one service |

See [The Basics](/concepts/).

## Regions

A project is pinned to a region. Default tenant region is `us-east-1`. Services in the same environment talk over private DNS; they do not cross projects on that network.

## Migrate and compare

<CardGrid>
  <LinkCard title="Migrate from Heroku" href="/platform/migrate-from-heroku/" description="Move the app, config vars, and add-ons with minimal downtime." />
  <LinkCard title="Compare to Railway" href="https://stackblaze.com/compare/railway" description="Canvas, services, and environments — on Kubernetes with tenant isolation." />
  <LinkCard title="Compare to Heroku" href="https://stackblaze.com/compare/heroku" description="Git deploys without dyno sleep." />
  <LinkCard title="Compare to Render" href="https://stackblaze.com/compare/render" description="One service model instead of a catalog of product types." />
</CardGrid>
